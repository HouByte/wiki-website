server:
  port: 8777

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    timeout: 20000
    client-name: pay
    jedis:
      pool:
        maxIdle: 5000
        maxWait: 500
        maxActive: 5000
        min-idle: 20
  datasource:
    name: catsquare
    url: jdbc:mysql://sql.bugio.cn:28266/bugio_wiki?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useSSL=false
    username: bugio_wiki
    password: bugio_wiki_2021
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    maxOpenPreparedStatements: 20

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true